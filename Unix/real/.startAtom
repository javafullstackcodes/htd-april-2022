#!/bin/bash

#check if the atom1 binary exist?

ls -l /usr/bin/atom1 >/dev/null 2>&1
if [ $? -eq 0 ]
then
	echo " Launching atom1 editor application .........."
	/usr/bin/atom1

else
	echo "atom1 application not found"
	exit 14
fi

